<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/result_card"
    android:layout_width="256dp"
    android:layout_height="256dp"
    android:layout_centerVertical="true"
    android:layout_centerHorizontal="true"
    android:background="@color/white">

    <RelativeLayout
        android:id="@+id/result_group"
        android:layout_width="match_parent"
        android:layout_height="204dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/result_card_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textColor="@color/experience_gray"
                android:lineSpacingMultiplier="1.1"
                android:gravity="center" />

            <Spinner
                android:id="@+id/result_card_spinner"
                android:layout_width="@dimen/units_spinner_layout_width"
                android:layout_height="@dimen/units_spinner_layout_height"
                android:gravity="center"
                android:visibility="gone" />
        </LinearLayout>

    </RelativeLayout>

    <Button
        android:id="@+id/result_card_button"
        android:layout_width="88dp"
        android:layout_height="36dp"
        android:layout_below="@id/result_group"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="@dimen/activity_vertical_margin"
        android:layout_marginEnd="@dimen/activity_horizontal_margin"
        android:text="@string/okButton"
        android:textAllCaps="true"
        android:fontFamily="sans-serif-medium"
        android:textSize="14sp"
        android:textColor="@color/courage_blue_light"
        android:background="@drawable/flatbutton_ripple"
        android:onClick="onClick_OkButton" />
</RelativeLayout>